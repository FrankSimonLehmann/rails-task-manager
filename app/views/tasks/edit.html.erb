<h1>Edit your task</h1>

<%= form_with model: @task, url: tasks_update_path do |f|%>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit %>
<% end %>

<%=link_to "back to all", tasks_all_path(@tasks) %>
